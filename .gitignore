node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz
.env
.env.local
.env.*.local
*.env
.env.smoke
.env.smoke.local

# Local utility scripts
_fix-*.cjs
_fix-*.js
scripts/.cache/

# Throwaway provider probe scripts (often contain hardcoded API keys —
# never commit). Local-only diagnostics for news/search providers.
server/scripts/probe-*

# IDE / agent workspace state
.claude/
.cursor/

# Local audit/debug outputs
tmp/

# Pre-migration data backups (contain user content — never commit)
backups/

# Temporary commit-msg file (PowerShell has no heredocs)
.git-commit-msg.tmp
.commit-msg.tmp
audit-*-output.json
dry-run-*-output.json

# phantom-diff diagnostic captures
phantom-capture-*.log

# World-market LLM calibration cache (re-runnable; the dated report
# is committed instead — see ops/world-market-calibration-*.md)
ops/.world-market-calibration-cache.json

# Capacitor: native projects are committed; generated copies and build output are not
android/.idea/
android/.gradle
android/build
android/app/build
android/local.properties
android/app/src/main/assets/public
android/app/src/main/assets/capacitor.config.json
android/app/src/main/assets/capacitor.plugins.json
ios/App/Pods
ios/App/App/public
ios/DerivedData
ios/App/build
*.xcuserstate
