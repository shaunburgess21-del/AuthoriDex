<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="com.voxdex.app" android:host="login" />
            </intent-filter>

            <!--
              Phase 7 HTTPS App Links. Apex only: www.voxdex.com 308s to
              https://voxdex.com/. Paths match share/referral URLs, not the
              whole host. autoVerify checks public/.well-known/assetlinks.json.
              The fingerprint there is the debug keystore (Pixel/debug only).
              Play signing is Phase 18. OAuth stays on the custom-scheme filter.
            -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:host="voxdex.com" android:path="/" />
                <data android:scheme="https" android:host="voxdex.com" android:path="/vote" />
                <data android:scheme="https" android:host="voxdex.com" android:path="/predict" />
                <data android:scheme="https" android:host="voxdex.com" android:pathPrefix="/person/" />
                <data android:scheme="https" android:host="voxdex.com" android:pathPrefix="/polls/" />
                <data android:scheme="https" android:host="voxdex.com" android:pathPrefix="/vote/matchups/" />
                <data android:scheme="https" android:host="voxdex.com" android:pathPrefix="/vote/opinion-polls/" />
                <data android:scheme="https" android:host="voxdex.com" android:pathPrefix="/predict/updown/" />
                <data android:scheme="https" android:host="voxdex.com" android:pathPrefix="/predict/h2h/" />
                <data android:scheme="https" android:host="voxdex.com" android:pathPrefix="/predict/race/" />
                <data android:scheme="https" android:host="voxdex.com" android:pathPrefix="/markets/" />
                <data android:scheme="https" android:host="voxdex.com" android:pathPrefix="/share/bet/" />
                <data android:scheme="https" android:host="voxdex.com" android:pathPrefix="/u/" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />
</manifest>
